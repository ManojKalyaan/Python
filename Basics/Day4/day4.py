import random

random_int = random.randint (0,5)

random_float = random.random ()

random_num = random_int + random_float

print (round (random_num,2))

#or (the below is the correct methord as above will generate only floats between 1 -5 not 0-5)

random5 = random.random()*5

print (random5)

import variables
z = variables.x + variables.y

print (z)

random_int1 = random.randint (0,1)

print (random_int1)